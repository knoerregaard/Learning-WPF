<Window x:Class="WPF_Ressourcer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Ressourcer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">


    <Grid>
        <Button IsEnabled="false" Style="{StaticResource ButtonStyle2}" Content="Knap">
            
            <!--<Button.Template>
                <ControlTemplate>
                    <Border BorderThickness="2" BorderBrush="Black" CornerRadius="10" Background="{TemplateBinding Background}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="275"></DropShadowEffect>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text=":)"></TextBlock>
                            <ContentPresenter Content="Tekst fra cp"></ContentPresenter>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Button.Template>-->
        </Button>
    </Grid>
</Window>
